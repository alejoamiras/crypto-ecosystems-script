# ====================
# GitHub Authentication
# ====================

# Primary GitHub token
GITHUB_TOKEN=ghp_your_primary_token_here

# Additional tokens for rotation (optional)
# Create multiple personal access tokens to rotate and avoid rate limits
GITHUB_TOKEN_1=ghp_your_second_token_here
GITHUB_TOKEN_2=ghp_your_third_token_here
GITHUB_TOKEN_3=ghp_your_fourth_token_here
# You can add up to GITHUB_TOKEN_10

# Enable token rotation to automatically switch between tokens
USE_TOKEN_ROTATION=false

# ====================
# Timeout Configuration
# ====================

# HTTP request timeout in milliseconds (default: 30000ms = 30 seconds)
# Used for individual API calls to GitHub
HTTP_REQUEST_TIMEOUT=30000

# Search operation timeout in milliseconds (default: 60000ms = 60 seconds)
# Maximum time for a single search query to complete (including retries)
SEARCH_TIMEOUT=60000

# ====================
# Retry Configuration
# ====================

# Maximum number of retry attempts for failed requests (default: 5)
MAX_RETRIES=5

# Base delay for rate limit retries in milliseconds (default: 10000ms = 10 seconds)
# When hitting rate limits without specific headers, delays will be:
# 1st retry: BASE_DELAY * 1 (10s)
# 2nd retry: BASE_DELAY * 2 (20s)
# 3rd retry: BASE_DELAY * 3 (30s), etc.
RATE_LIMIT_BASE_DELAY=10000

# Maximum delay between retries in milliseconds (default: 90000ms = 90 seconds)
# Retries won't wait longer than this, even if rate limit suggests longer
MAX_RETRY_DELAY=90000

# Delay for standard (non-rate-limit) retries in milliseconds (default: 1000ms = 1 second)
# Uses exponential backoff: 1s, 2s, 4s, 8s, etc.
STANDARD_RETRY_BASE_DELAY=1000

# ====================
# Rate Limiting
# ====================

# Delay between processing repositories in milliseconds (default: 100ms)
# Helps avoid triggering GitHub's abuse detection
REPO_PROCESSING_DELAY=100

# Delay between search queries in milliseconds (default: 2000ms = 2 seconds)
# Prevents rapid-fire searches that might trigger rate limits
SEARCH_QUERY_DELAY=2000

# ====================
# Logging Configuration
# ====================

# Log level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# Write logs to file instead of console (default: false)
LOG_TO_FILE=false
